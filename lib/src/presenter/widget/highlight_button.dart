import 'package:flutter/material.dart';

class HighlightButton extends StatelessWidget {
  const HighlightButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
